<% for (detail of DETAILS.map(detail => detail.tableName).reverse()) {%>
from .<%= detail %>_schema import <%= toClassName(detail) + 'Schema' %>

<% } %>


from .<%= toFileName(PAGE_TITLE) %>_schema import <%= toClassName(PAGE_TITLE) + 'Schema' %>

from .parameter import <%= toClassName(PAGE_TITLE) + 'ParameterSchema' %>

