<%
 function toClassName(str) {
     return str
         .replace(/[^a-zA-Z0-9]+/g, '_')
         .split(/[\s_-]+/)
         .map(word => word.charAt(0).toUpperCase() + word.slice(1).toLowerCase())
         .join('');
 }
 const PAGE_TITLE = SCHEMA.pageTitle
%>